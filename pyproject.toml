[project]
name = "time-for-school"
version = "0.2.1"
description = "Home Assistant integration: 'time to leave for school' alert that turns things off and blinks lights on a weekly schedule."
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [{ name = "mvheimburg" }]

[project.urls]
Homepage = "https://github.com/mvheimburg/time-for-school"
Issues = "https://github.com/mvheimburg/time-for-school/issues"

# The version above is the release source of truth. CI verifies that
# custom_components/time_for_school/manifest.json carries the same version,
# and the release workflow tags v<version> when it changes on main.

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["E501"]
